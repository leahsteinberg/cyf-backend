export const handlePush = async (req: Request, res: Response) => {

    const pushToken = '';
    const userId = '';
    console.log("register push --", req.body)
    res.send("GOT THE PUSH TOKEN!")

}